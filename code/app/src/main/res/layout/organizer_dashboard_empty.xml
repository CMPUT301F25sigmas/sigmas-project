<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"
    tools:context=".OrganizerDashboardActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/FilterAllButton"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginHorizontal="6dp"
            android:layout_weight="1"
            android:backgroundTint="@color/light_purple"
            android:text="All"
            android:textColor="@color/text_color"
            android:padding="0dp"
            app:cornerRadius="8dp"/>
        <Button
            android:id="@+id/filterActivebutton"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:backgroundTint="@color/background_grey"
            android:text="Active"
            android:textColor="@color/text_color"
            android:padding="0dp"
            app:cornerRadius="8dp"/>

        <Button
            android:id="@+id/filterClosedButton"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:backgroundTint="@color/background_grey"
            android:text="Closed"
            android:textColor="@color/text_color"
            android:padding="0dp"
            app:cornerRadius="8dp"/>

        <Button
            android:id="@+id/filterOngoingButton"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:backgroundTint="@color/background_grey"
            android:text="On"
            android:textColor="@color/text_color"
            android:padding="0dp"
            app:cornerRadius="8dp"/>

        </LinearLayout>

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            app:cardBackgroundColor="#F2F2F2"
            app:cardCornerRadius="24dp"
            app:cardElevation="0dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="10dp">

            <!-- Events List (shown when there are events) -->
            <ScrollView
                android:id="@+id/events_scroll_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="16dp"
                android:visibility="gone">

                <LinearLayout
                    android:id="@+id/events_container_organizer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <!-- Events will be dynamically added here -->

                </LinearLayout>

            </ScrollView>

            <!-- Empty State (shown when there are no events) -->
            <LinearLayout
                android:id="@+id/empty_state"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:visibility="gone">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="24dp"
                    android:text="No Active Events to\ndisplay right now."
                    android:textAlignment="center"
                    android:textColor="#494949"
                    android:textSize="16sp" />

                <Button
                    android:id="@+id/create_event_button"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Create Event"
                    app:backgroundTint="@color/light_purple"
                    app:cornerRadius="16dp" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/empty_state_filtered"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="vertical"
                android:visibility="gone">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="24dp"
                    android:text="No events matching filters."
                    android:textAlignment="center"
                    android:textColor="#494949"
                    android:textSize="16sp" />
            </LinearLayout>

        </FrameLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>